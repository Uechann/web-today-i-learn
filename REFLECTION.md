# 생각해 보기 - 미션 회고록

## HTML

### 1. 나만의 갤러리에서 사진 9장은 HTML 태그 중 어떤 요소로 표현하는 것이 좋을까? 이유는 뭐라고 생각했는가?
나만의 갤러리 이미지는 `<img>` 태그를 사용하거나, 이미지와 설명을 함께 담기 위해 `<figure>`와 `<figcaption>` 조합을 사용하는 것이 좋습니다. 이번 미션에서는 단순 그리드 배치를 위해 `<img>`를 사용했습니다. 9장의 사진은 서로 연관된 데이터의 집합이므로, 시멘틱하게는 `<ul>`과 `<li>`로 감싸서 리스트 형태로 구조화하는 것이 가장 적절합니다. 이는 검색 엔진이나 스크린 리더가 해당 영역을 "순서가 있는 혹은 없는 이미지들의 목록"으로 정확히 인식하게 도와주기 때문입니다.

### 2. 뼈대 코드를 기반으로 이미지를 로드하는 방식은 크게 2가지로 나뉘는데 무엇이고 어떻게 사용하고 있는가?
이미지를 로드하는 방식은 크게 **HTML `<img>` 태그**를 사용하는 방식과 **CSS `background-image` 속성**을 사용하는 방식으로 나뉩니다.
- **`<img>` 태그**: 이미지 자체가 콘텐츠의 중요한 부분일 때 사용하며, `alt` 속성을 통해 접근성을 제공합니다. 이번 미션의 프로필 사진과 갤러리 이미지에 사용되었습니다.
- **CSS `background-image`**: 이미지가 디자인적인 장식 요소이거나, 텍스트의 배경으로 쓰일 때 사용합니다. 이번 미션에서는 특정 영역의 레이아웃이나 패턴을 넣을 때 활용될 수 있습니다.

### 3. 시맨틱 태그(예: <header>, <main>, <section>, <footer> 등)로 문서를 구분할 때, 어떤 기준으로 섹션을 나누고 태그를 적용하는 것이 좋을까?
문서의 논리적 구조와 콘텐츠의 독립성을 기준으로 나눕니다.
- `<header>`: 페이지나 섹션의 도입부, 로고, 내비게이션을 담습니다.
- `<nav>`: 주요 링크 모음(메뉴)에 사용합니다.
- `<main>`: 페이지의 핵심 콘텐츠가 시작되는 단 한 곳에 사용합니다.
- `<section>`: 주제별로 연관된 콘텐츠를 그룹화할 때 사용하며, 보통 내부에 제목(`<h2>`~`<h6>`)을 포함합니다.
- `<article>`: 블로그 포스트나 뉴스 기사처럼 그 자체로 독립적인 내용일 때 사용합니다.
- `<footer>`: 저작권 정보, 연락처 등 하단 정보를 담습니다.

### 4. <img> 태그의 alt 속성은 왜 필요하며, 어떻게 작성하는 것이 좋을까?
`alt` 속성은 웹 접근성을 위해 필수적입니다. 시각 장애인이 스크린 리더를 사용할 때 이미지의 내용을 설명해주고, 네트워크 오류로 이미지를 불러오지 못할 때 텍스트로 대신 보여줍니다. 작성 시에는 단순히 "이미지"라고 적기보다, "베이스 기타를 연주하는 모습"처럼 이미지의 의미를 구체적으로 전달하도록 작성해야 합니다.

### 5. 이미지를 로드할 때, 상대 경로(relative path)와 절대 경로(absolute path)를 혼용하면 어떤 문제가 발생할 수 있을까?
절대 경로는 외부 서버의 리소스를 가져올 때 유용하지만, 도메인이 바뀌거나 외부 서버가 중단되면 리소스를 잃게 됩니다. 반면 상대 경로는 프로젝트 내부 구조에 의존하므로 로컬 개발 환경과 배포 환경이 일치해야 합니다. 혼용 시, 특히 배포 단계에서 경로 참조 오류(404)가 발생할 확률이 높고 프로젝트의 이식성이 떨어집니다.

### 6. <meta> 태그의 viewport 설정과 Open Graph 태그는 각각 어떤 역할을 하는가?
- **Viewport**: 모바일 기기에서 화면의 너비와 배율을 조절하여 반응형 웹이 정상적으로 보이게 합니다. (`width=device-width, initial-scale=1`)
- **Open Graph (OG)**: 카카오톡, 페이스북 등 소셜 미디어에 링크를 공유할 때 미리보기 제목, 설명, 이미지를 제어하여 클릭률을 높이는 마케팅적 역할을 합니다.

---

## CSS

### 1. 나만의 갤러리에서 9장의 이미지를 보기 좋게 배치하려면, Flexbox와 CSS Grid 중 어떤 것이 더 적합할까? 그 이유는?
**CSS Grid**가 더 적합합니다. Flexbox는 1차원 레이아웃(행 또는 열 하나)에 최적화되어 있지만, 9장의 사진을 3x3 형태의 2차원 그리드로 배치하는 데는 Grid가 훨씬 직관적이고 코드가 간결합니다. `grid-template-columns: repeat(3, 1fr);` 한 줄로 완벽한 격자 구조를 만들 수 있기 때문입니다.

### 2. 태그 선택자, 클래스 선택자, ID 선택자는 각각 언제 사용하는 것이 적절할까?
- **태그 선택자**: 모든 `p`, `h2` 등에 공통으로 적용할 기본 스타일(폰트, 여백 등)을 정의할 때 사용합니다.
- **클래스 선택자(`.`)**: 여러 요소에서 공통으로 사용할 디자인 패턴(예: `.card`, `.btn`)을 정의할 때 가장 많이 사용합니다.
- **ID 선택자(`#`)**: 페이지 내에서 유일한 요소에 스타일을 주거나, JavaScript에서 특정 요소를 빠르게 찾기 위한 갈고리(Hook) 역할을 할 때 사용합니다.

### 3. CSS를 HTML 내에 직접 작성하는 것과 별도 파일로 분리하는 것의 차이는 무엇일까?
- **내부 작성**: 소규모 페이지에서 관리가 편하지만, 코드가 길어지면 가독성이 떨어지고 재사용이 어렵습니다.
- **별도 파일 분리**: HTML과 디자인을 분리하여 유지보수가 쉽고, 브라우저가 CSS 파일을 캐싱(Caching)할 수 있어 페이지 로딩 속도가 빨라집니다.

---

## JavaScript

### 1. document.getElementById()와 document.querySelector()의 차이는 무엇인가?
- **`getElementById`**: 오직 ID값으로만 요소를 찾으며 성능이 매우 빠릅니다.
- **`querySelector`**: CSS 선택자 문법(`#`, `.`, `tag > tag`)을 그대로 사용할 수 있어 훨씬 유연하고 강력하지만, 성능 면에서는 `getElementById`보다 약간 느릴 수 있습니다. 현대적인 개발에서는 범용성 때문에 `querySelector`를 선호합니다.

### 2. 이벤트 리스너를 사용해서 "어떤 일이 일어났을 때 어떤 동작을 실행"하게 만들 수 있다. 자기소개 페이지에서 활용할 수 있는 이벤트에는 어떤 것들이 있을까?
- `click`: 갤러리 이미지 클릭 시 모달창으로 확대 보기.
- `submit`: TIL 작성 폼 제출 시 리스트 업데이트.
- `scroll`: 스크롤 시 상단 내비게이션 배경색 변경 또는 "Top" 버튼 표시.
- `mouseover`: 프로필 사진에 마우스를 올렸을 때 다른 이미지로 변경.

### 3. innerHTML로 DOM을 조작할 때 주의해야 할 점은 무엇일까?
가장 큰 주의점은 **XSS (Cross-Site Scripting) 공격**입니다. 사용자로부터 입력받은 값을 필터링 없이 `innerHTML`로 넣으면 악의적인 스크립트가 실행될 수 있습니다. 또한, `innerHTML`은 기존 요소를 모두 지우고 새로 그리기 때문에 성능 저하가 발생할 수 있으며, 기존에 걸려있던 이벤트 리스너가 사라질 수 있습니다. 단순 텍스트는 `textContent`를 사용하는 것이 안전합니다.

---

## 배포

### 1. GitHub Pages로 배포한 페이지에서 TIL을 작성하면, 새로고침하거나 재배포했을 때 데이터가 유지될까? 그 이유는?
**유지되지 않습니다.** GitHub Pages는 정적(Static) 웹 호스팅 서비스로, 서버 측 데이터베이스가 없습니다. JavaScript로 추가한 데이터는 브라우저의 메모리상에만 존재하므로 페이지를 새로고침하면 초기 상태로 돌아갑니다.

### 2. JavaScript로 추가한 데이터를 영구적으로 저장하려면 어떤 방법이 있을까?
- **브라우저 저장소**: `LocalStorage`나 `IndexedDB`를 사용하여 사용자 기기에 데이터를 저장합니다. (기기/브라우저가 바뀌면 보이지 않음)
- **외부 데이터베이스**: Firebase, Supabase와 같은 BaaS(Backend as a Service)를 연동하거나, 별도의 API 서버를 구축하여 DB에 저장합니다.
- **GitHub API**: GitHub Actions 등을 활용해 커밋 형태로 데이터를 파일에 저장할 수도 있습니다.
